openai>=1.40.0
Pillow>=10.3.0
python-dotenv>=1.0.0

# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.29.0
httpx>=0.27.0

# ── SAM 2 (optional — only needed when USE_SAM2=true) ────────────────────────
# Segment Anything Model 2 — universal auto-segmentation (logos + photos)
# Model checkpoint (~300 MB) downloaded automatically on first run via huggingface_hub.
# Install PyTorch first (see Real-ESRGAN section below), then:
#   pip install sam2 huggingface_hub
sam2>=1.0.0
huggingface_hub>=0.23.0

# ── rembg (optional — only needed when REMOVE_BG=true) ───────────────────────
# Background removal via U2Net model (~170 MB, downloaded on first run)
#   pip install rembg onnxruntime
rembg>=2.0.50
onnxruntime>=1.17.0

# ── vtracer (optional — only needed when USE_VTRACER=true) ───────────────────
# Rust-based color vectorizer; better gradient handling than Inkscape trace.
#   pip install vtracer
vtracer>=0.6.0

# ── Real-ESRGAN (optional — only needed when USE_REALESRGAN=true) ─────────────
# Install PyTorch first (CPU-only build, ~200 MB):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Then install the rest:
#   pip install realesrgan basicsr facexlib gfpgan opencv-python numpy
numpy>=1.26.0
opencv-python>=4.9.0
realesrgan>=0.3.0
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
# torch and torchvision must be installed separately (see above) because the
# correct wheel depends on whether you have a CUDA GPU or CPU-only.

# ── System dependency (not a Python package) ──────────────────────────────────
#   Inkscape >= 1.2  — https://inkscape.org/release/
#   Required for PNG -> SVG tracing and SVG -> EPS export.
